<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Main Activity Text Views -->
    <style name="TextView">
        <item name="android:layout_height">88dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">22sp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- location_list.xml TextViews background-->
    <style name="TextViewLocation">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Green Background with alpha value of 70% -->
    <color name="alpha_green">#B333691E</color>

    <!-- Red Background with alpha value of 70% -->
    <color name="alpha_red">#B3BF360C</color>

    <!-- Sky Blue background with alpha value of 70%-->
    <color name="alpha_sky_blue">#B3006064</color>

    <!-- Brown background with alpha value of 70% -->
    <color name="alpha_brown">#B33E2723</color>

    <!-- Tab Layout Style -->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">?attr/colorAccent</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">?android:textColorPrimaryInverse</item>
        <item name="tabBackground">?attr/colorPrimary</item>
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondaryInverse</item>
        <item name="textAllCaps">true</item>
    </style>


</resources>
